services:
  app:
    build: .
    volumes:
      - app-data:/app/data
      - app-uploads:/app/uploads
    environment:
      - PRESENTATION_TTL_DAYS=21
    restart: unless-stopped

volumes:
  app-data:
  app-uploads:
